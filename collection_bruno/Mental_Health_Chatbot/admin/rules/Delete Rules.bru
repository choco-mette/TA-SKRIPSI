meta {
  name: Delete Rules
  type: http
  seq: 4
}

delete {
  url: {{server}}/api/v1/admin/rules/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
