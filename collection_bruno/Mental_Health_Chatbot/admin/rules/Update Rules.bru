meta {
  name: Update Rules
  type: http
  seq: 3
}

put {
  url: {{server}}/api/v1/admin/rules/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
  "content": "netest",
  "is_active": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
