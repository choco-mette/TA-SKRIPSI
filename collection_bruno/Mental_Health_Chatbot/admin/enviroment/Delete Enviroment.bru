meta {
  name: Delete Enviroment
  type: http
  seq: 4
}

delete {
  url: {{server}}/api/v1/admin/environments/6
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
