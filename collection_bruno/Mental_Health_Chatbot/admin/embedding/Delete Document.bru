meta {
  name: Delete Document
  type: http
  seq: 2
}

delete {
  url: {{server}}/api/v1/admin/documents/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
