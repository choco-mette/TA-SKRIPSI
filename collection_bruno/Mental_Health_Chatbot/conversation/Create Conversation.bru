meta {
  name: Create Conversation
  type: http
  seq: 1
}

post {
  url: {{server}}/api/v1/conversations/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
